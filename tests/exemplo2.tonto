package ErrosSintaticos

// --- ERRO LÉXICO 1: Caractere Inválido ---
# Lexer deve capturar o caractere '$'
$

// --- ERRO SINTÁTICO 1: Nomenclatura Quebrada (R2) ---
// Parser espera CLASS_NAME (Maiúscula), mas recebe RELATION_NAME (minúscula)
kind pessoa {
    nome: string
}

// --- ERRO SINTÁTICO 2: Palavra Reservada no Atributo (R2) ---
// O Parser não espera uma Palavra Reservada (KW_KIND) como nome de atributo
kind Objeto {
    kind: string 
}

// --- ERRO SINTÁTICO 3: Generalização em Bloco Incompleta (R5) ---
// Falta 'specifics' (estrutura incorreta)
disjoint complete genset GensetInvalido {
    general Objeto 
    // Falha aqui
}

// --- ERRO SINTÁTICO 4: Relação Externa Incompleta (R6) ---
// Falta o alvo final da relação (Classe 2), quebrando a sintaxe
@mediation relation Objeto [1] -- [1]